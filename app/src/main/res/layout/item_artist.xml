<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">
    <data>

        <import type="iam.thevoid.epic.myapplication.data.model.musicbrnz.Artist" />
        <variable
            name="artist"
            type="Artist" />
        <variable
            name="vm"
            type="iam.thevoid.epic.myapplication.presentation.ui.artist.ArtistViewModel" />
    </data>
    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="8dp">

            <FrameLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bckg_gray_corners_8dp"
                android:padding="8dp">

                <ImageView
                    android:id="@+id/flag"
                    app:imageUrl="@{vm.flag(artist)}"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    tools:src="@tools:sample/avatars" />

            </FrameLayout>

            <LinearLayout
                android:orientation="vertical"
                android:layout_gravity="center_vertical"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" >
                <TextView
                    android:id="@+id/title"
                    android:text="@{artist.name}"
                    android:textColor="#2b2b2b"
                    android:textSize="16sp"
                    android:fontFamily="sans-serif-medium"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_vertical"
                    android:layout_marginStart="12dp"
                    tools:text="[epic.rt.ru]" />
                <TextView
                    android:id="@+id/subtitle"
                    android:text="@{artist.disambiguation == null ? Artist.EMPTY_DISAMBIGUATION : artist.disambiguation}"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center_vertical"
                    android:layout_marginStart="12dp"
                    tools:text="[epic.rt.ru]" />

            </LinearLayout>
        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_gravity="bottom"
            android:layout_marginStart="8dp"
            android:background="#ccc" />

    </FrameLayout>
</layout>